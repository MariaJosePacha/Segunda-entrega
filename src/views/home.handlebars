<h1>Mis productos</h1>
<p>Crear una vista "home.handlebars" la cual contenga una lista de todos los productos agregados hasta el momento</p>

<div class="contenerdorCards">
    {{#each productos}}
        <div class="card" data-id="{{this.id}}">
            <p>{{this.title}}</p>
            <p>{{this.description}}</p>
            <p>{{this.price}}</p>
            <p>{{this.category}}</p>
            <p>{{this.stock}}</p>
            
        </div>
    {{/each}}
</div>

<script src="js/index.js"></script> 
